[pytest]
testpaths = src/tests
addopts = -v --tb=short
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning

[tool:pytest]
# Coverage: pytest src/tests/ --cov=src --cov-report=html --cov-fail-under=70
